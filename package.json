{
  "name": "@meteora-digital/filters",
  "version": "0.0.1",
  "description": "This will act as our filtering engine. We will pass in filters, Then call apply(). An ajax (No this doesn't use jQuery) request will be sent in the form of URL parameters, the response will be returned to the gobal scope.",
  "main": "dist/js/filters.js",
  "scripts": {
    "test": "echo \"Error: no test specified\""
  },
  "keywords": [
    "JavaScript",
    "es6",
    "filters",
    "frontend",
    "URL",
    "parameters",
    "ajax"
  ],
  "author": "Emmanuel Davies",
  "license": "MIT",
  "dependencies": {
    "meteora": "latest"
  },
  "devDependencies": {
    "@babel/core": "^7.10.2",
    "@babel/preset-env": "^7.10.2",
    "gulp": "^4.0.2",
    "gulp-babel": "^8.0.0",
    "gulp-plumber": "^1.2.1"
  }
}
